<Window
    x:Class="SeedSeeker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SeedSeeker"
    Title="Seed Seeker">
  <Grid x:Name="Root">
    <Grid.RowDefinitions><RowDefinition Height="48"/><RowDefinition/><RowDefinition Height="28"/></Grid.RowDefinitions>

    <!-- Unified title bar drawn over Mica -->
    <Grid x:Name="AppTitleBar">
      <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center" Margin="18,0,0,0">
        <FontIcon Glyph="&#xE721;" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
        <TextBlock Text="Seed Seeker" Style="{StaticResource CaptionTextBlockStyle}" VerticalAlignment="Center"/>
      </StackPanel>
    </Grid>

    <Grid Grid.Row="1">
      <!-- The web design's pane tracks: query 1fr, results 0.8fr, scout 0.8fr,
           so the sidebar shares in the window's width instead of staying at a
           fixed size while the other panes soak up all the slack. The island's
           MinWidth is the two inner minimums plus their divider. -->
      <Grid.ColumnDefinitions><ColumnDefinition Width="*" MinWidth="318"/><ColumnDefinition Width="1.6*" MinWidth="681"/></Grid.ColumnDefinitions>

      <!-- Query sidebar, directly on Mica -->
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <TextBlock Text="Query" Style="{StaticResource SubtitleTextBlockStyle}" Margin="20,2,20,10"/>
        <ScrollView Grid.Row="1" HorizontalScrollMode="Disabled"
                    HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
          <StackPanel Padding="20,0,20,20">
            <TextBlock Text="Presets" Style="{StaticResource SectionHeader}" Margin="1,2,0,8"/>
            <StackPanel Spacing="6">
              <ComboBox x:Name="PresetPicker" PlaceholderText="Load a preset" DisplayMemberPath="Name"
                        HorizontalAlignment="Stretch" SelectionChanged="PresetPicker_SelectionChanged"/>
              <Grid ColumnSpacing="6">
                <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <Button Grid.Column="0" x:Name="SavePresetButton" Click="SavePreset_Click" HorizontalAlignment="Stretch">
                  <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE74E;" FontSize="13"/><TextBlock Text="Save current query"/>
                  </StackPanel>
                </Button>
                <Button Grid.Column="1" x:Name="CopyLinkButton" Click="CopyLink_Click"
                        AutomationProperties.Name="Copy query link"
                        ToolTipService.ToolTip="Copy a shareable link to the current query">
                  <FontIcon x:Name="CopyLinkIcon" Glyph="&#xE71B;" FontSize="13"/>
                </Button>
                <Button Grid.Column="2" x:Name="DeletePresetButton" Click="DeletePreset_Click"
                        ToolTipService.ToolTip="Delete selected saved preset" IsEnabled="False">
                  <FontIcon Glyph="&#xE74D;" FontSize="13"/>
                </Button>
              </Grid>
            </StackPanel>
            <TextBlock Text="Requirements" Style="{StaticResource SectionHeader}" Margin="1,2,0,8"/>
            <!-- The requirement board: one chip per requirement, the members of
                 an either/or cluster sharing a dashed capsule, stack badges at
                 the trailing edge. The code-behind rebuilds it from
                 QueryRelationships.BoardItems on every change; dragging a chip
                 onto another makes an either/or, and dropping one on the board
                 itself takes it out of its cluster. The drag is the board's
                 own, driven by pointer events (see the board section of the
                 code-behind); its ghost chip rides in DragLayer at the end. -->
            <local:WrapPanel x:Name="RequirementBoard" Spacing="6" LineSpacing="8" MinHeight="34" Background="Transparent"/>
            <!-- Only there while a chip is in flight: the pointer's way to delete. -->
            <Border x:Name="RemoveZone" Visibility="Collapsed"
                    Margin="0,8,0,0" Padding="0,8" CornerRadius="10" BorderThickness="1"
                    Background="Transparent" BorderBrush="{ThemeResource SystemFillColorCriticalBrush}">
              <StackPanel Orientation="Horizontal" Spacing="6" HorizontalAlignment="Center">
                <FontIcon x:Name="RemoveZoneIcon" Glyph="&#xE74D;" FontSize="12" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                <TextBlock x:Name="RemoveZoneLabel" Text="drop to remove" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
              </StackPanel>
            </Border>
            <TextBlock x:Name="NoRequirements" Text="No requirements yet. Add one to describe the item you're hunting for." Style="{StaticResource Caption}" Margin="1,0,1,10"/>
            <!-- The four setting sections sit two to a row while the sidebar is
                 wide enough for it, so the wider sidebar buys less scrolling
                 rather than emptier cards; SettingsGrid_SizeChanged moves the
                 cells back into one column when it is not. Each cell is a
                 header over a card that stretches to its row, so the two cards
                 of a row share a bottom edge. -->
            <Grid x:Name="SettingsGrid" ColumnSpacing="12" SizeChanged="SettingsGrid_SizeChanged">
              <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition/></Grid.ColumnDefinitions>
              <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

              <Grid x:Name="ScopeCell">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                <TextBlock Text="Search scope" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="1" Style="{StaticResource SettingsCard}">
                  <StackPanel>
                    <Grid ColumnSpacing="8">
                      <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                      <TextBlock Text="Floor limit" VerticalAlignment="Center"/>
                      <TextBlock Grid.Column="1" x:Name="FloorLabel" Style="{StaticResource Caption}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <!-- Indexes into FloorLimits.Options (skips the empty boss floors); the code-behind
                         installs a ThumbToolTipValueConverter so the tooltip names the floor, not the index. -->
                    <Slider x:Name="FloorSlider" StepFrequency="1" ValueChanged="FloorSlider_ValueChanged" HorizontalAlignment="Stretch" Margin="0,2,0,-8"/>
                  </StackPanel>
                </Border>
              </Grid>

              <Grid x:Name="WandmakerCell" Grid.Column="1">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                <TextBlock Text="Wandmaker" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="1" Style="{StaticResource SettingsCard}">
                  <Grid ColumnSpacing="14">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                    <TextBlock Text="Quest" VerticalAlignment="Center"/>
                    <ComboBox Grid.Column="1" x:Name="WandmakerQuestPicker" HorizontalAlignment="Stretch" MinWidth="0" SelectionChanged="WandmakerQuestChanged"/>
                  </Grid>
                </Border>
              </Grid>

              <Grid x:Name="BlacksmithCell" Grid.Row="1">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                <TextBlock Text="Blacksmith" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="1" Style="{StaticResource SettingsCard}">
                  <StackPanel Spacing="10">
                    <Grid ColumnSpacing="14">
                      <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                      <TextBlock Text="Require accessible blacksmith" TextWrapping="Wrap" VerticalAlignment="Center"/>
                      <ToggleSwitch Grid.Column="1" x:Name="RequireBlacksmith" Style="{StaticResource CompactToggle}" Toggled="SettingChanged"/>
                    </Grid>
                    <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}"/>
                    <Grid ColumnSpacing="14" RowSpacing="4">
                      <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                      <Grid.RowDefinitions><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
                      <TextBlock Text="Exclude Smith rewards" TextWrapping="Wrap" VerticalAlignment="Center"/>
                      <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="Required items cannot come from the 2,000-favor Smith choice, leaving favor available for reforging." Style="{StaticResource Caption}"/>
                      <ToggleSwitch Grid.Column="1" x:Name="ExcludeRewards" Style="{StaticResource CompactToggle}" Toggled="SettingChanged" VerticalAlignment="Top" Margin="0,-4,0,0"/>
                    </Grid>
                  </StackPanel>
                </Border>
              </Grid>

              <!-- The worker slider is the whole cell, so a single-core machine
                   has nothing to show here: LoadWorkerPreference collapses it. -->
              <Grid x:Name="PerformanceCell" Grid.Row="1" Grid.Column="1">
                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
                <TextBlock Text="Performance" Style="{StaticResource SectionHeader}"/>
                <Border Grid.Row="1" Style="{StaticResource SettingsCard}">
                  <StackPanel>
                    <Grid ColumnSpacing="8">
                      <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/></Grid.ColumnDefinitions>
                      <TextBlock Text="Workers" VerticalAlignment="Center"/>
                      <TextBlock Grid.Column="1" x:Name="WorkerLabel" Style="{StaticResource Caption}" TextWrapping="NoWrap" TextTrimming="CharacterEllipsis" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                    <Slider x:Name="WorkerSlider" StepFrequency="1" Minimum="1" ValueChanged="WorkerSlider_ValueChanged" HorizontalAlignment="Stretch" Margin="0,2,0,-4"/>
                    <TextBlock Text="Number of search threads to spawn." Style="{StaticResource Caption}"/>
                  </StackPanel>
                </Border>
              </Grid>
            </Grid>

            <TextBlock Text="Challenges" Style="{StaticResource SectionHeader}"/>
            <Button Style="{StaticResource CardButton}" Click="Challenges_Click">
              <Grid ColumnSpacing="12">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <FontIcon Glyph="&#xE7C1;" FontSize="16" VerticalAlignment="Center" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                <StackPanel Grid.Column="1" Spacing="2" VerticalAlignment="Center">
                  <TextBlock Text="Challenges"/>
                  <TextBlock Text="Simulate runs with challenges enabled" Style="{StaticResource Caption}"/>
                </StackPanel>
                <TextBlock x:Name="ChallengeSummary" Grid.Column="2" Style="{StaticResource Caption}" VerticalAlignment="Center"/>
                <FontIcon Grid.Column="3" Glyph="&#xE76C;" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" VerticalAlignment="Center"/>
              </Grid>
            </Button>
          </StackPanel>
        </ScrollView>
        <StackPanel Grid.Row="2" Padding="20,14,20,10" Spacing="10" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
          <Button x:Name="StartButton" Style="{StaticResource AccentButtonStyle}" HorizontalAlignment="Stretch" Padding="0,9" Click="Start_Click">
            <StackPanel Orientation="Horizontal" Spacing="8">
              <FontIcon x:Name="StartIcon" Glyph="&#xE768;" FontSize="14"/>
              <TextBlock x:Name="StartLabel" Text="Start Search"/>
            </StackPanel>
            <Button.KeyboardAccelerators><KeyboardAccelerator Modifiers="Control" Key="Enter"/></Button.KeyboardAccelerators>
          </Button>
          <HyperlinkButton x:Name="UpstreamLink" Click="About_Click" FontSize="12"
                           HorizontalAlignment="Center" Padding="6,2"
                           ToolTipService.ToolTip="Artwork attribution and licenses"/>
        </StackPanel>
      </Grid>

      <!-- Content island: Results + Seed Detail on a raised layer, Windows 11 Settings style -->
      <Grid Grid.Column="1" Background="{ThemeResource LayerFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1,1,0,0"
            CornerRadius="8,0,0,0">
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="*" MinWidth="320"/>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*" MinWidth="360"/>
        </Grid.ColumnDefinitions>

        <!-- Results -->
        <Grid Padding="24,18,24,0">
          <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
          <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <TextBlock Text="Results" Style="{StaticResource SubtitleTextBlockStyle}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
              <!-- Download/Upload glyphs, so the arrow directions match the
                   other platforms: into the app for import, out of it for export. -->
              <Button x:Name="ImportResultsButton" Style="{StaticResource SubtleLabelButton}" Click="ImportResults_Click" ToolTipService.ToolTip="Import results from a file">
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <FontIcon Glyph="&#xE896;" FontSize="14" VerticalAlignment="Center"/>
                  <TextBlock Text="Import" VerticalAlignment="Center"/>
                </StackPanel>
              </Button>
              <Button x:Name="ExportResultsButton" Style="{StaticResource SubtleLabelButton}" Click="ExportResults_Click" ToolTipService.ToolTip="Export results to a file">
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <FontIcon Glyph="&#xE898;" FontSize="14" VerticalAlignment="Center"/>
                  <TextBlock Text="Export" VerticalAlignment="Center"/>
                </StackPanel>
              </Button>
              <!-- Discards the results and the run they came from, so the next
                   Start Search scans from the beginning instead of refining. -->
              <Button x:Name="ClearResultsButton" Style="{StaticResource SubtleLabelButton}" Click="ClearResults_Click" ToolTipService.ToolTip="Clear the results so the next search starts fresh">
                <StackPanel Orientation="Horizontal" Spacing="6">
                  <FontIcon Glyph="&#xE894;" FontSize="14" VerticalAlignment="Center"/>
                  <TextBlock Text="Clear" VerticalAlignment="Center"/>
                </StackPanel>
              </Button>
            </StackPanel>
          </Grid>
          <TextBlock x:Name="SearchStatus" Grid.Row="1" Text="Add requirements, then press Start Search." Style="{StaticResource Caption}" MinHeight="32" Margin="0,10,0,10"/>
          <Grid Grid.Row="2" Padding="12,0,12,8" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition/></Grid.ColumnDefinitions>
            <TextBlock Text="#" Style="{StaticResource Caption}"/>
            <TextBlock Grid.Column="1" Text="Seed" Style="{StaticResource Caption}"/>
          </Grid>
          <ListView x:Name="ResultsList" Grid.Row="3" Margin="-12,4,-12,0" Padding="12,0,12,16"
                    ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                    SelectionChanged="ResultsList_SelectionChanged" DoubleTapped="ResultsList_DoubleTapped">
            <ListView.ItemContainerStyle>
              <Style TargetType="ListViewItem">
                <Setter Property="MinHeight" Value="36"/>
                <Setter Property="Padding" Value="12,0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
              </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
              <DataTemplate>
                <Grid>
                  <Grid.ColumnDefinitions><ColumnDefinition Width="40"/><ColumnDefinition/></Grid.ColumnDefinitions>
                  <TextBlock Text="{Binding Number}" Style="{StaticResource Caption}" VerticalAlignment="Center"/>
                  <TextBlock Grid.Column="1" Text="{Binding Seed}" FontFamily="Cascadia Mono, Consolas" VerticalAlignment="Center"/>
                  <Grid.ContextFlyout>
                    <MenuFlyout>
                      <MenuFlyoutItem Text="Copy seed" Click="CopyResult_Click">
                        <MenuFlyoutItem.Icon><FontIcon Glyph="&#xE8C8;"/></MenuFlyoutItem.Icon>
                      </MenuFlyoutItem>
                      <MenuFlyoutItem Text="Scout seed" Click="ScoutResult_Click">
                        <MenuFlyoutItem.Icon><FontIcon Glyph="&#xE721;"/></MenuFlyoutItem.Icon>
                      </MenuFlyoutItem>
                    </MenuFlyout>
                  </Grid.ContextFlyout>
                </Grid>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </Grid>

        <Border Grid.Column="1" Width="1" Background="{ThemeResource DividerStrokeColorDefaultBrush}"/>

        <!-- Seed detail -->
        <Grid Grid.Column="2">
          <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/></Grid.RowDefinitions>
          <StackPanel Padding="24,18,24,14" Spacing="10" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <TextBlock Text="Seed Detail" Style="{StaticResource SubtitleTextBlockStyle}" Margin="0,0,0,4"/>
            <Grid ColumnSpacing="8">
              <Grid.ColumnDefinitions><ColumnDefinition/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
              <TextBox x:Name="SeedInput" PlaceholderText="AAA-AAA-AAA" FontFamily="Cascadia Mono, Consolas" FontSize="17"
                       TextChanged="SeedInput_TextChanged" KeyDown="SeedInput_KeyDown"/>
              <Button Grid.Column="1" x:Name="ScoutButton" Content="Scout" Click="Scout_Click" VerticalAlignment="Stretch"/>
              <Button Grid.Column="2" Content="Copy" Click="CopySeed_Click" VerticalAlignment="Stretch"/>
            </Grid>
            <TextBlock x:Name="ScoutStatus" Style="{StaticResource Caption}" Margin="0,-2,0,0"/>
            <!-- One chip per quest of the scouted seed, e.g. "Great crab · Sad Ghost · F4"; built in code-behind. -->
            <local:WrapPanel x:Name="QuestStrip" Spacing="8" LineSpacing="4" Visibility="Collapsed"/>
            <!-- Shown while the scouted seed is one of the search results: J/K (or the chevrons) step through them. -->
            <StackPanel x:Name="ResultNav" Orientation="Horizontal" Spacing="8" Visibility="Collapsed">
              <Button x:Name="PrevResultButton" Click="PrevResult_Click" Padding="7,3"
                      AutomationProperties.Name="Previous result"
                      ToolTipService.ToolTip="Scout the previous search result (K)">
                <FontIcon Glyph="&#xE76B;" FontSize="11"/>
              </Button>
              <TextBlock x:Name="ResultPosition" Style="{StaticResource Caption}" VerticalAlignment="Center"/>
              <Button x:Name="NextResultButton" Click="NextResult_Click" Padding="7,3"
                      AutomationProperties.Name="Next result"
                      ToolTipService.ToolTip="Scout the next search result (J)">
                <FontIcon Glyph="&#xE76C;" FontSize="11"/>
              </Button>
              <TextBlock Text="J / K" Style="{StaticResource Caption}" VerticalAlignment="Center" Opacity="0.6"/>
            </StackPanel>
          </StackPanel>

          <Grid Grid.Row="1">
            <StackPanel x:Name="EmptyScout" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="8" MaxWidth="320" Margin="24,0">
              <FontIcon Glyph="&#xE707;" FontSize="36" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
              <TextBlock Text="No seed scouted" Style="{StaticResource BodyStrongTextBlockStyle}" HorizontalAlignment="Center"/>
              <TextBlock Text="Enter a canonical seed, or select a search result, to inspect its item manifest." Style="{StaticResource Caption}" TextAlignment="Center"/>
            </StackPanel>
            <ListView x:Name="ScoutList" Visibility="Collapsed" SelectionMode="None" Padding="12,4,12,16"
                      ScrollViewer.HorizontalScrollMode="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Hidden">
              <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                  <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                  <Setter Property="Padding" Value="12,2"/>
                </Style>
              </ListView.ItemContainerStyle>
              <ListView.GroupStyle>
                <GroupStyle>
                  <GroupStyle.HeaderTemplate>
                    <DataTemplate>
                      <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBlock Text="{Binding Floor}" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <local:FloorFeelingView Feeling="{Binding Feeling}" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding Region}" Style="{StaticResource Caption}" VerticalAlignment="Bottom" Margin="0,0,0,1"/>
                        <!-- The floor's quest variant, or empty (rendering nothing) on quest-less floors. -->
                        <TextBlock Text="{Binding Quest}" Style="{StaticResource Caption}" VerticalAlignment="Bottom" Margin="0,0,0,1"/>
                      </StackPanel>
                    </DataTemplate>
                  </GroupStyle.HeaderTemplate>
                </GroupStyle>
              </ListView.GroupStyle>
              <ListView.ItemTemplate>
                <DataTemplate>
                  <Grid ColumnSpacing="10" Padding="0,6">
                    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <local:SpriteView SpriteIndex="{Binding SpriteIndex}" TypeIconIndex="{Binding TypeIconIndex}" SpriteSize="22"
                                      GlowColor="{Binding GlowColor}" GlowPeriod="{Binding GlowPeriod}"
                                      VerticalAlignment="Top" Margin="0,2,0,0"/>
                    <StackPanel Grid.Column="1" Spacing="2">
                      <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{Binding ItemName}" FontWeight="{Binding Weight}" TextWrapping="Wrap"/>
                        <TextBlock Text="{Binding Upgrade}" Visibility="{Binding UpgradeVisibility}" Foreground="{ThemeResource SystemFillColorSuccessBrush}" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        <Border Visibility="{Binding CurseVisibility}" Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}" CornerRadius="8" Padding="7,1" VerticalAlignment="Center">
                          <TextBlock Text="cursed" FontSize="11" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
                        </Border>
                        <Border Visibility="{Binding SecretVisibility}" Background="{ThemeResource SystemFillColorAttentionBackgroundBrush}" CornerRadius="8" Padding="7,1" VerticalAlignment="Center"
                                ToolTipService.ToolTip="Hidden in a secret room — search to reveal it">
                          <TextBlock Text="secret" FontSize="11" Foreground="{ThemeResource SystemFillColorAttentionBrush}"/>
                        </Border>
                      </StackPanel>
                      <StackPanel Orientation="Horizontal" Spacing="6">
                        <TextBlock Text="{Binding Effect}" Visibility="{Binding EffectVisibility}" Foreground="{Binding EffectBrush}" FontSize="12"/>
                        <TextBlock Text="·" Visibility="{Binding EffectVisibility}" Style="{StaticResource Caption}"/>
                        <TextBlock Text="{Binding Source}" Style="{StaticResource Caption}"/>
                      </StackPanel>
                      <TextBlock Text="{Binding Accessibility}" Visibility="{Binding AccessibilityVisibility}" FontSize="11" Foreground="{ThemeResource TextFillColorTertiaryBrush}" TextWrapping="Wrap"/>
                    </StackPanel>
                    <Border Grid.Column="2" Visibility="{Binding MatchVisibility}" Background="{ThemeResource SystemFillColorSuccessBackgroundBrush}" CornerRadius="10" Padding="8,2" VerticalAlignment="Top" Margin="8,2,0,0">
                      <StackPanel Orientation="Horizontal" Spacing="5">
                        <FontIcon Glyph="&#xE73E;" FontSize="10" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                        <TextBlock Text="Match" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                      </StackPanel>
                    </Border>
                    <ContentControl Grid.Row="1" Grid.ColumnSpan="3" Content="{Binding TrinketDeck}" HorizontalContentAlignment="Stretch"/>
                  </Grid>
                </DataTemplate>
              </ListView.ItemTemplate>
            </ListView>
          </Grid>
        </Grid>
      </Grid>
    </Grid>

    <!-- Window-bottom status bar for transient refine and result-cap notices,
         drawn directly on Mica like the sidebar so it reads as window chrome.
         It stays in place when empty, the native pattern, which also spares the
         content island a layout jump whenever a notice appears. -->
    <Grid Grid.Row="2" Padding="24,0" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
      <TextBlock x:Name="StatusBarText" Style="{StaticResource Caption}" HorizontalAlignment="Right" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
    </Grid>

    <!-- Above everything and transparent to the pointer: the ghost chip that
         follows a requirement drag rides here, placed in Root coordinates. -->
    <Canvas x:Name="DragLayer" Grid.RowSpan="3" IsHitTestVisible="False"/>
  </Grid>
</Window>
