Accepted repeated-item pruning: incremental c47e2f5 → 2ab235c Linux measurements.

- `summary.json` and `TABLE.md` retain every throughput cell and independently recomputed aggregate; `samples.jsonl` and `timings.csv` preserve all 128 scored timing/signature observations.
- `design.json` retains exact queries, inputs, commands, setup receipts and protocol metadata. `provenance.json` binds build/source/binary and audit/raw input hashes. `environment.jsonl` retains lightweight raw counters and derived observations, omitting named process lists.
- `planner/` retains all 14 setup cases, raw warm and first-call nanoseconds, process receipts, signed pair changes, calibration counts and build/source provenance. `queries.json.gz` is the exact original query attachment, losslessly compressed; its uncompressed hash is in `planner/completion.json`. Run `gzip -cd planner/queries.json.gz` to inspect it. Setup regressions are retained. Warm includes destruction; first call excludes separately timed destruction. These are initialized processes, not application cold starts.
- `validation.json` binds the integrated source to the tested snapshot, records complete correctness receipts and commands, all workspace test summaries, the larger differential, fixed floor traces and the separate full-returned-record adapter differential. Full nonmatching worlds are outside the adapter comparison.
- `codegen/` contains actual normal-release instruction inspection and explicitly normalized diffs, with raw binary/code references. It does not establish the cause of the control regressions.
- `publication.json` records the keep decision and code commit. `SHA256SUMS` covers every published file except itself. Verify from this directory with `sha256sum -c SHA256SUMS`.

Full original recipe/witness arrays, correctness transcripts, binaries, source archives and profiles remain local, hash-bound by the receipts. Their absence from this compact export is explicit. Compact signature equality references the full completed audit, not a new full-world proof. CLI equality is count-only. Query planning is excluded from matching seconds and is measured separately. Full timed adapter wire stdout and warmup arrays were not separately retained; lifecycle receipts establish completion, not an independent wire transcript.

Original ae0cb0c → fbee876 checkpoint results remain separate; their gains must not be multiplied by this incremental result. No sample was excluded for regression or environmental observations. Different input counts across worker counts measure throughput, not fixed-input strong scaling. New measurements are Linux-only on a shared VM, with no new Mac or other-target throughput claim.
